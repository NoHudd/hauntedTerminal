# HFSE Docs

Index of design and reference docs. Repo-level guidance is in the root `CLAUDE.md`;
the player/story intro is in the root `README.md`.

- **REWRITE_PLAN.md** — the typed-core rewrite (Strangler): what `engine/` is, phases
  done, and the committed direction to retire `src/`. Read for architecture.
- **DIFFICULTY_SIM_STATUS.md** — current balance state and the sim harness findings.
- **extending_the_game.md** — how to author rooms/items/enemies/npcs in YAML.
- **TODO.md** — architecture backlog (open items: flatten item YAML, dynamic room paths, …).
- **art_rec.md** — art/visual recommendations (forward-looking).
- **UX_REDESIGN.md** — UX redesign notes (forward-looking).
